server:
  port: 8088

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pratheeban233/spring-cloud-config-repo
          clone-on-start: true
          skip-ssl-validation: true